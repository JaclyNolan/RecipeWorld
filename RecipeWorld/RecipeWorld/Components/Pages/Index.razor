@attribute [Route(RecipeWorld.Constants.RouteNames.Index)]
@using RecipeWorld.Constants
@inject NavigationManager Navigation

<h3>Redirecting...</h3>

@code {
    protected async override Task OnInitializedAsync()
    {
        base.OnInitialized();
        // Redirect to another page or URL
        Navigation.NavigateTo(RouteNames.Recipe.List);
    }
}